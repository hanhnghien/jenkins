@Library(['kv-shared-library']) _

pipeline {
  agent none
  stages {
    stage ('Hello, World!') {
      steps {
        script {
          hello 'Kiot'
          hello 'Viet'
        }
      }
    }
  }
}
